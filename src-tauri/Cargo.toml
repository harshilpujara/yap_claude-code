[package]
name = "flow"
version = "0.1.0"
description = "Flow - free voice-to-text for Windows"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
